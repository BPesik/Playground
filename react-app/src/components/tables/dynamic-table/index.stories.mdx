import Table from "./index";
import { Props } from "@storybook/addon-docs/blocks";

<Meta title="Table" 
    component={Table} 
    argTypes={{}}
/>

export const Template = (args) => <Table {...args} />;

export const rows = [
    {cells: [{value: "Bram"},{value: "He likes to program"},{value: "Busy"},]},
    {cells: [{value: "Peter"},{value: "Always doing stuff and likes to talk"},{value: "Available"},]},
    {cells: [{value: "John"},{value: "John will be John"},{value: "Nobody knows"}]}
];

export const header = {
    cells:[{value: "Name"}, {value: "Description"}, {value: "Status"}]
};

<Canvas>
    <Story name="Primary" args={{dataRows: rows, dataHeader: header}}>
        {Template.bind({})}
    </Story>
</Canvas> 

# Table

The table requires the following properties:

<Props of={Table} />
