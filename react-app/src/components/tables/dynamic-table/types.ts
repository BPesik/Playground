export type DataCell = {
    value: string;
};

export type DataRow = {
    cells: DataCell[];
};

export type DataHeaderCell = {
    value: string;
};

export type DataHeader = {
    cells: DataHeaderCell[];
};
