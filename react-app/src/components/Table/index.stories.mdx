import Table from './index';
import { Props } from '@storybook/addon-docs/blocks';
import MockedData from "../../MockData/todos"

<Meta title="Table" 
    component={Table} 
    argTypes={{}}
/>

export const Template = (args) => <Table {...args} />;

<Canvas>
    <Story name="Material UI Table" args={{title: "Material UI Table", rows: MockedData({numOfTodos: 10})}}>
        {Template.bind({})}
    </Story>
</Canvas>

# Table

The Material UI table requires the following properties:

<Props of={Table} />

